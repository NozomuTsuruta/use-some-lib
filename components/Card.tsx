import React from 'react'
import styled from 'styled-components'

const Card = () => {
  return (
    <CardContainer>
      <Button/>
    </CardContainer>
  )
}

export default Card

const CardContainer = styled.div`

`
